extends ../layout
//- extend to pobranie calego layoutu i napidsze block content - nazwa przpadkowa ale tu i tu ta sama/// ponizej:
//- /// to sie wywola bo w indes js w routes wysowla to na req lub res? ?????

block content
  h1= title
  nav
    ul
      li  
        a(href='/admin/news/add') Dodaj artykul
  p Welcome to #{title}
  table
    tr
      th id
      th tytuł
      th data dodania
      th akcja
    each item, index in data
      tr
        td
          td=index
          td=item.title
          td=item.created
          td
            a(href=`/admin/news/delete/${item.id}`) Usun

//- w p jest w #{} bo to string i jak w JSX